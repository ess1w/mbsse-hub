# Production dependencies — tested on Python 3.11
# Excludes Celery, Redis, boto3, SendGrid (not needed for prototype)

# Web framework
fastapi>=0.111.0,<0.200
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.36,<3
asyncpg>=0.29.0
alembic>=1.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0.0,<5          # passlib works with bcrypt 4.x on Python 3.11

# Validation & settings
pydantic>=2.7,<3
pydantic-settings>=2.2,<3
python-multipart>=0.0.9
email-validator>=2.1

# Scheduler (in-process async, no Redis required)
apscheduler>=3.10,<4

# Utilities
python-dotenv>=1.0

# Async HTTP (Preset guest token requests)
httpx>=0.27.0

# File storage
cloudinary>=1.40.0
