# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.36       # 2.0.36+ required for Python 3.13 support
asyncpg>=0.29.0          # async PostgreSQL driver
alembic>=1.13.1

# Auth
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4              # password hashing
bcrypt==3.2.2                       # pin to 3.x — passlib 1.7.4 not compatible with bcrypt 4.x

# Validation & settings
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9             # file upload form parsing
email-validator==2.1.1

# File storage (S3-compatible; works with AWS S3 / Render Disks / MinIO)
boto3==1.34.84

# Background tasks & scheduling
celery==5.4.0
redis==5.0.4                        # celery broker
flower==2.0.1                       # celery monitoring (optional)

# Scheduler (in-process async, no Redis required)
apscheduler>=3.10,<4

# Email
sendgrid==6.11.0

# Utilities
python-dotenv==1.0.1
httpx==0.27.0                       # async HTTP (for testing)

# Dev / test
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
factory-boy==3.3.0
